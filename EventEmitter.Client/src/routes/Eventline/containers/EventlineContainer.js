import { connect } from 'react-redux'
import EventlineView from '../components/EventlineView'

const mapDispatchToProps = (state) => ({
})

const mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(EventlineView)
